# Terminal Practice

Safe non-destructive drills:

- identify the current directory with `pwd`;
- list contents with `ls`;
- navigate using relative paths;
- navigate using one absolute path;
- identify repository root versus project root;
- inspect an environment variable;
- explain why a normal coding task should not require `sudo`.

Do not use deletion, permission changes or administrator commands as beginner drills.
