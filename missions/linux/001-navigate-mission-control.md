# 🖥️ Mission LNX-01 — Navigate Mission Control

**Track:** Linux & terminal

## Where am I?

The Mission Control repository in the integrated terminal.

## What am I learning?

Current directory, relative paths, parent directories and the difference between repository root and project root.

## What do I do next?

1. Run `pwd` in Mission Control.
2. Run `ls` and identify `START-HERE.md`, `missions`, `learning` and `progress`.
3. Use `cd ../projects/python-basics` or `cd ../projects/snake-game` to enter one independent project.
4. Run `pwd` and `git status` there and explain why this is a different Git repository.
5. Return to Mission Control without closing the terminal.
6. Ask Antigravity to explain one relative path and one absolute path from the session.

## How do I run it?

Use only non-destructive navigation commands: `pwd`, `ls` and `cd`.

## How do I know it worked?

Connor finishes back at the repository root and can identify where he is without guessing.

## Connor decides

Connor chooses which project directory to enter.

## Tracker evidence

Do **not** promote capability merely because Antigravity completed the work.

Potential evidence from this mission:

- DIRECT DEMONSTRATION: correct navigation.
- EXPLANATION: current directory and relative/absolute path difference.

## Harder challenge

Reach `../projects/python-basics/tests` from Mission Control using relative paths, then return to Mission Control in two commands or fewer.
